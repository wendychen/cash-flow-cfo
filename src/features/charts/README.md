# Charts Feature

Contains all visualization components used in the dashboard.

## Components
- SankeyFlowChart
- CombinedChart
- MonthlySummary
- ConsciousnessFlow

> Note: `ExpenseChart` lives in `@/features/expenses` (it is expense-specific).

These components are designed to react to the selected time period from the Time Navigator.
