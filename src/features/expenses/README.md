# Expenses Feature

Handles variable expenses and fixed/recurring expenses.

## Components
- ExpenseForm / ExpenseList
- ExpenseChart
- FixedExpenseForm / FixedExpenseList (currently colocated in savings feature for now)

Expenses support period filtering via the Time Navigator.
